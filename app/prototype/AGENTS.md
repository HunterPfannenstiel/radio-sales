# Prototype

The prototype directory is where the user goes to piece together components into a feature that will be ready for integration with the whole application.

Each directory represents a feature being built.

A directory will contain both mocked and completed components.
