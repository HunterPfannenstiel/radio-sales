Standards files contain rules and decisions, not token values. Token values are already
in `globals.css` — never redefine them inline or in new CSS.

Start with [navigation-guide.md](navigation-guide.md) for a full indexed view of
all standards files. Read the descriptions and pull only the files that match your
task — most tasks touch 2-3 files at most. Don't read everything.
